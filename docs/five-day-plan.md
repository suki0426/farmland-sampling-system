# 五日开发与答辩计划

当前只有 5 个完整开发日，加最后半天冻结和答辩彩排。

## Day 1：打通底层数据链

- 设备组：三台 Python 终端独立发送数据。
- 组长：Java 后端统一接收并提供基础 API。
- 数据库：完成核心表初稿，可保存设备数据。
- 前端：高德地图可打开，预留三个设备 Marker。
- 算法：完成规则网格和 Point-in-Polygon。

验收：三台发送 → 一个接收 → 数据可保存 → 前端可获取最新设备数据。

## Day 2：完成实时演示链

- 总数据约 100 条，三台设备各约 33～34 条。
- 建议每约 2 秒发送 1 条。
- 三台设备 Marker 实时变化。
- 右侧最新数据实时刷新，数据库实时增长，设备历史可查询。
- 算法继续完成蛇形路线和 Nearest Neighbor。

验收：三发一收 + MySQL + 地图三个点实时更新。

## Day 3：完成课题本体

- 农田 Polygon 绘制与保存。
- 按间距 / 数量生成采样点及 Marker。
- 路线规划与展示。
- 显示设备当前位置和下一采样点导航逻辑。

验收：圈农田 → 自动布点 → 生成路线 → 显示采样员位置。

## Day 4：完成业务闭环

采样点弹窗至少支持采样深度、土壤类型、土壤湿度、pH、现场描述、采样时间和采样人员。

状态流转：未采样 → 当前目标 → 已完成。

完成采样记录入库、历史查询和基础统计；有余力再做 NMEA 与 2-opt。

验收：规划 → 导航 → 采样 → 保存 → 完成 → 历史可查。

## Day 5：稳定与联调

禁止大改架构。优先处理 Bug 修复、前后端联调、2-opt、历史轨迹、ECharts、Excel 报表和 UI 优化。

NDVI 只有核心系统完全稳定后才能开发；核心仍有 Bug 时直接取消 NDVI。

晚上冻结 develop，完成全链路演示测试并准备答辩数据。

## 最后半天：冻结与彩排

禁止新增功能，只允许修致命 Bug、制作 PPT、准备演示脚本和进行彩排。

现场安排：1 人讲 PPT，3 人分别控制 3 台发送终端，组长控制中央服务器和 Web 平台。

演示主线：画农田 → 自动布点 → 路线规划 → 三终端上传 → 地图三个点实时更新 → 完成采样 → 查看历史 / 统计。
