<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeeplus.sys.mapper.OfficeMapper">
    <select id="getList" resultType="com.jeeplus.sys.service.vo.OfficeVO">
        SELECT o.id AS "id",
        o.name AS "name",
        o.type AS "type",
        u.no AS "masterNo",
        u.id AS "masterId",
        u.name AS "master"
        FROM sys_office o
        LEFT JOIN sys_user u ON o.master = u.id
        WHERE o.del_flag = 0 and o.grade = 2
        <if test="keyWord != null and keyWord != ''">
            and o.name LIKE CONCAT('%', #{keyWord}, '%')
        </if>
    </select>

    <select id="getIdsByMaster" resultType="java.lang.String">
        select id
        FROM sys_office
        WHERE del_flag =0 and master = #{userid}
    </select>
</mapper>
