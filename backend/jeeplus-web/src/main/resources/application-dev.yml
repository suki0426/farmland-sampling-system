server:
  port: 8087
  servlet:
    context-path:
  tomcat:
    uri-encoding: UTF-8
    basedir: ${user.dir}/.tomcat
    accesslog:
      pattern: common
      enabled: true
      directory: logs
      prefix: jeeplus_access_log
      suffix: .log
      request-attributes-enabled: true
      rename-on-rotate: true
    max-http-form-post-size: 200MB
  jetty:
    max-http-form-post-size: 20000000B
logging:
  level:
    root: INFO
    #    org.flowable: DEBUG
    com.jeeplus: DEBUG
    org.apache.shiro.cache.ehcache.EhCacheManager: WARN
integration:
  device-udp:
    # 开发联调默认开启；生产环境由部署配置显式决定。
    enabled: true
    port: 9000
    max-track-size: 1000
  algorithm:
    mode: mock
spring:
  servlet:
    multipart:
      maxFileSize:  1000MB
      maxRequestSize: 1000MB
  devtools:
    restart:
      enabled: true
  #  profiles:
  #    active: dev
  autoconfigure:
    exclude: com.alibaba.druid.spring.boot.autoconfigure.DruidDataSourceAutoConfigure
  #  jpa:
  #    open-in-view: false
  datasource:
    dynamic:
      druid: # 全局druid参数，绝大部分值和默认保持一致。(现已支持的参数如下,不清楚含义不要乱设置)
        SelectMethod: cursor
        filters: stat # 注意这个值和druid原生不一致，默认启动了stat,wall
        validation-query:  SELECT 'x'
      p6spy: false # 默认false,线上必须关闭。开发阶段可以打开调试输出mybatis语句，但是有许多莫名其妙的bug，以及严重的性能问题，所以正式环境必须关闭。
      primary: master
      datasource:
        master:
          username: ${MYSQL_USERNAME:root}
          password: ${MYSQL_PASSWORD:}
          url: jdbc:p6spy:mysql://${MYSQL_HOST:127.0.0.1}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:farmland_sampling_db}?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
          driver-class-name: com.mysql.cj.jdbc.Driver

          #oracle database settings
          #          username: ${EXAMPLE_USERNAME:}
          #          password: ${EXAMPLE_PASSWORD:}
          #          url: jdbc:oracle:thin:@localhost:1521:helowin
          #          driver-class-name: oracle.jdbc.driver.OracleDriver

          ##mssql database settings
          #          username: ${EXAMPLE_USERNAME:}
          #          password: ${EXAMPLE_PASSWORD:}
          #          driver-class-name: net.sourceforge.jtds.jdbc.Driver
          #          url: jdbc:jtds:sqlserver://localhost:1433;SelectMethod=cursor;DatabaseName=jeeplus
          #          SelectMethod: cursor

          ##pg database settings
  #              username: ${EXAMPLE_USERNAME:}
  #              password: ${EXAMPLE_PASSWORD:}
  #              driver-class-name: org.postgresql.Driver
  #              url: jdbc:postgresql://localhost:54321/jeeplus-vue-pg
  #              testSql: SELECT 'x'
  # if you need use pg database , please enable this config.
  #    jpa:
  #      properties:
  #        hibernate:
  #          jdbc:
  #            lob:
  #              non_contextual_creation: true

  #  redis的配置
  redis:
    host: ${REDIS_HOST:127.0.0.1}
    port: ${REDIS_PORT:6379}
    expireTime: 3600000 #单位秒
    database: 1
  #    password:${EXAMPLE_PASSWORD:}
  mvc.servlet.load-on-startup: 1
  jmx:
    enabled: false
  # quartz任务配置
  quartz:
    job-store-type: memory
    properties:
      org:
        quartz:
          scheduler:
            instanceName: clusteredScheduler
            instanceId: AUTO
          jobStore:
            class: org.quartz.simpl.RAMJobStore
          threadPool:
            class: org.quartz.simpl.SimpleThreadPool
            threadCount: 10
            threadPriority: 5
            threadsInheritContextClassLoaderOfInitializingThread: true
  # jackson配置
#  jackson:
#    default-property-inclusion: NON_NULL
#    time-zone: GMT+8
#    date-format: yyyy-MM-dd HH:mm:ss
flowable:
  # 关闭定时任务Job
  async-executor-activate: true
  check-process-definitions: false
  process-definition-location-prefix: classpath:/processes/
  database-schema-update: false
  common:
    app:
      idm-url: ${FLOWABLE_IDM_URL:http://127.0.0.1:8087/flowable-idm}
      idm-admin:
        user: admin
        password: ${FLOWABLE_IDM_PASSWORD:}
#mybatis-plus配置
mybatis-plus:
  config-location: classpath:/mybatis/mybatis-config.xml
  mapper-locations:
    - classpath*:com/jeeplus/**/*Mapper.xml
    - classpath:/META-INF/admin-mybatis-mappings/*.xml
    - classpath:/META-INF/modeler-mybatis-mappings/*.xml
  #实体扫描，多个package用逗号或者分号分隔
  typeAliasesPackage: com.jeeplus.**.domain
  configuration-properties:
    prefix:
    boolValue: TRUE
    blobType: BLOB
  type-handlers-package: com.jeeplus.core.mapper
#  global-config:
#    db-config:
#      logic-removeWithChildrenById-field: delFlag  # 全局逻辑删除的实体字段名(since 3.3.0,配置后可以忽略不配置步骤2)
#      logic-removeWithChildrenById-value: 1 # 逻辑已删除值(默认为 1)
#      logic-not-removeWithChildrenById-value: 0 # 逻辑未删除值(默认为 0)

#是否开启 swagger，生产环境请关闭
swagger:
  enable: true

#cas服务端的地址
cas:
  #  server-url-prefix: https://www.cainiao.com:8443/cas
  server-url-prefix: ${CAS_SERVER_URL:}
#wps配置
wps:
  appid: ${WPS_APP_ID:}
  appsecret: ${WPS_APP_SECRET:}
  download_host: ${WPS_DOWNLOAD_HOST:}
  domain: ${WPS_DOMAIN:}
  downloadCallbackPath: /usr/local/wps
  webctx: /jeeplus-vue

#演示模式
demoMode: false

#oss配置
config:
  accessory:
    #local, aliyun, minIO, unicom
    type: local
    baseDir: local
    local:
      location: D:\Develop\Dev\WordGet\GET\userfiles
    aliyun:
      endpoint:
      accessKeyId: ${ALIYUN_OSS_ACCESS_KEY_ID:}
      accessKeySecret: ${ALIYUN_OSS_ACCESS_KEY_SECRET:}
      bucketName:
    minIO:
      endpoint:
      accessKey: ${MINIO_ACCESS_KEY:}
      secretKey: ${MINIO_SECRET_KEY:}
      bucketName:
    unicom:
      endpoint:
      accessKey: ${UNICOM_OSS_ACCESS_KEY:}
      secretKey: ${UNICOM_OSS_SECRET_KEY:}
      bucketName:
      publicEndPoint:
#上传文件类型配置
file:
  allowedType: file  # 允许上传的文件类型， all, file ,image, audio, video, office
  extensions:
    all: all       # 允许上传所有类型文件
    file: 7z,aiff,asf,avi,bmp,csv,doc,docx,fla,flv,gif,gz,gzip,jpeg,jpg,mid,mov,mp3,mp4,mpc,mpeg,mpg,ods,odt,pdf,png,ppt,pptx,pxd,qt,ram,rar,rm,rmi,rmvb,rtf,sdc,sitd,swf,sxc,sxw,tar,tgz,tif,tiff,txt,vsd,wav,wma,wmv,xls,xlsx,zip       # 只允许上传安全文件（linux系统非可执行）
    image: gif,jpg,jpeg,bmp,png     # 只允许上传图片
    audio: CD,OGG,MP3,ASF,WMA,WAV,MP3PRO,RM,REAL,APE,MODULE,MIDI,VQF    # 只允许上传音频
    video: AVI,WMV,RM,RMVB,MPEG1,MPEG2,MPEG4(MP4),3GP,ASF,SWF,VOB,DAT,MOV,M4V,FLV,F4V,MKV,MTS,TS     # 只允许上传视频
    office: txt,xls,xlsx,xlsm,xltx,xltm,xlsb,xlam,doc,docx,docm,dotx,dotm,ppt,pptx,pptm,ppsx,ppsm,potx,potm,ppam     # 只允许上传office文件
#JWT access token过期时间（access过期时间，并不是指需要重新登录的超时时间，而是指需要刷新重新获取access token的时间，超时登录的时间是3*EXPIRE_TIME，即refresh token的有效时时间)
jwt.accessToken.expireTime: 86400  # 86400 ，24*3600* 24小时，单位秒


#===============================#
#====== license 信息 ================#
#===============================#
# 开发者全局信息配置，优先级高于数据库中的配置，如果不启用将采用数据库中配置
devtool:
  enabled: true      # 是否启用本地配置, true/false
  projectPath: D:\GeniusGjq\Code\AI-Box\AI-Box-Java\jeeplus-module\jeeplus-aishop        #后端生成路径
  frontPath: D:\GeniusGjq\Code\AI-Box\AI-Box-Vue    # 前端生成路径
  packageName: com.jeeplus.aishop    # 默认包名
  author: GeniusGjq     # 默认作者
# 产品授权
productId: Y2021041773
# dell
license: 50722C3E25E6D8CF9E2B3EF0B308FA9EE635DA3010E9EFF608A406E518BFA1CC44417AA424180D32FB63113805BC51DEC5BF17AF92289DC07F15AADB987384D6
# redmi
#license: 5DBD960E22D0A5AADD9BB6CD571F16BCF4B1351D98934D3D33507276ACEBCD7737ABB35349A3299832CDBDBA145D91DFFEC4248B861CBFAB49B3121C79168A3D

#mongo-plus:
mongo-plus:
  enabled: false
  #  configuration:
  #    auto-create-time-series: true #开启自动创建时序集合功能
  #    auto-create-index: true #开启自动创建索引功能
  #    autoConvertObjectId: true
  #    collection:
  #      mappingStrategy: CLASS_NAME
  #      block-attack-inner: true
  #  data:
  #    mongodb:
  #      host: 152.136.36.2
  #      port: 27017
  #      username: ${EXAMPLE_USERNAME:}
  #      password: ${EXAMPLE_PASSWORD:}
  #      database: iot_dev
  #      connectTimeoutMS: 50000 #在超时之前等待连接打开的最长时间（以毫秒为单位）
  #      authenticationDatabase: iot_dev
  #  data:
  #    mongodb:
  #      host: 81.70.194.158
  #      port: 3306
  #      username: ${EXAMPLE_USERNAME:}
  #      password: ${EXAMPLE_PASSWORD:}
  #      database: iot_dev
  #      connectTimeoutMS: 50000 #在超时之前等待连接打开的最长时间（以毫秒为单位）
  #      authenticationDatabase: iot_dev

  log: true
  format: true
  pretty: true

#websocket 多个设备使用逗号分隔
websocket:
  enabled: true  # 设置为 true 来启用自动连接设备，false 则跳过连接
  devices:
    ids: device001
    addresses: ${WEBSOCKET_DEVICE_ADDRESS:ws://127.0.0.1:9527}

influxdb:
  url: ${INFLUXDB_URL:http://127.0.0.1:8086}
  token: ${INFLUXDB_TOKEN:}
  bucket: door_radar_status
  org: quanquan
  username: ${INFLUXDB_USERNAME:}
  password: ${INFLUXDB_PASSWORD:}
  influxdbSwitch: false # 设置为true加载Influxdb服务类，设置为false则跳过
mqtt:
  brokerUrl: ${MQTT_BROKER_URL:tcp://127.0.0.1:1883}
  clientId: iot-device-002
  username: ${MQTT_USERNAME:}
  password: ${MQTT_PASSWORD:}
  mqttSwitch: false # 设置为true加载mqtt服务类，设置为false则跳过

verify:
  secretKey: ${VERIFY_SECRET_KEY:}
